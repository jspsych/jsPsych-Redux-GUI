// This is the initial state of the store.
import { Timeline, InitialState } from 'guiState';
// Any new features of the store should be addded here.

export default function trialState (state = {}, action) {
    if (state === null) {
        return { InitialState };
    }
    default:
        return state;
    }
}
