import React from 'react';
import { render } from 'react-dom';

const Trial = ({
    name,
    children,
    type,
    pluginType,
    pluginData,
    errors
}) => {
<div>
<title>{name}</title>
</div>
}